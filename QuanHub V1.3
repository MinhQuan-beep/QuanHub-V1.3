-- [[ ================================================= ]] --
-- [[               QUÂN HUB PHIÊN BẢN V1.3              ]] --
-- [[         ĐÃ LƯU GHIM: CẤM XÓA DÒNG - CẤM BỎ CODE     ]] --
-- [[       PHÁT TRIỂN BỞI: QUAN_BLACKLOCK & IRO        ]] --
-- [[ ================================================= ]] --

local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

---------------------------------------------------------
-- [ HỆ THỐNG CÁC DỊCH VỤ CƠ BẢN ] --
---------------------------------------------------------
local Players = game:GetService("Players")
local player = Players.LocalPlayer
local UIS = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local Lighting = game:GetService("Lighting")
local TeleportService = game:GetService("TeleportService")
local HttpService = game:GetService("HttpService")
local VirtualUser = game:GetService("VirtualUser")

---------------------------------------------------------
-- [ THIẾT LẬP CỬA SỔ CHÍNH ] --
---------------------------------------------------------
local Window = Rayfield:CreateWindow({
   Name = "Quân Hub | V1.3",
   LoadingTitle = "Hệ Thống Quân Hub Đang Khởi Chạy...",
   LoadingSubtitle = "Chào mừng Admin Iro trở lại",
   ConfigurationSaving = {
      Enabled = true,
      FolderName = "QuanHub_V13_Data",
      FileName = "MainConfig"
   },
   KeySystem = true,
   KeySettings = {
      Title = "Hệ Thống Bảo Mật Quân Hub",
      Subtitle = "Xác Minh Quyền Truy Cập",
      Note = "Mã truy cập là: Top1_Script",
      FileName = "QuanHubKeySystem",
      SaveKey = true,
      Key = {"Top1_Script"} 
   }
})

---------------------------------------------------------
-- [ TẠO CÁC DANH MỤC TÍNH NĂNG ] --
---------------------------------------------------------
local MainTab = Window:CreateTab("Chính", 4483362458)
local ExtraTab = Window:CreateTab("Phụ", 4483362458)
local ServerTab = Window:CreateTab("Máy Chủ", 4483362458)
local SettingTab = Window:CreateTab("Cài Đặt", 4483345998)

---------------------------------------------------------
-- [[ 1. TAB CHÍNH: 12 TÍNH NĂNG GỐC ]] --
---------------------------------------------------------
MainTab:CreateSection("Vận Động Hệ")

MainTab:CreateButton({
    Name = "1. Nhảy Vô Hạn",
    Callback = function()
        UIS.JumpRequest:Connect(function()
            if player.Character and player.Character:FindFirstChildOfClass("Humanoid") then
                player.Character:FindFirstChildOfClass('Humanoid'):ChangeState("Jumping")
            end
        end)
        Rayfield:Notify({Title = "Thành công", Content = "Đã bật Nhảy Vô Hạn", Duration = 2})
    end
})

MainTab:CreateButton({
    Name = "2. Xuyên Tường (Noclip)",
    Callback = function()
        _G.Noclip = true
        RunService.Stepped:Connect(function()
            if _G.Noclip then
                if player.Character then
                    for _, v in pairs(player.Character:GetDescendants()) do
                        if v:IsA("BasePart") then
                            v.CanCollide = false
                        end
                    end
                end
            end
        end)
        Rayfield:Notify({Title = "Thành công", Content = "Đã bật Xuyên Tường", Duration = 2})
    end
})

MainTab:CreateButton({
    Name = "3. Hiện Người Chơi (ESP)",
    Callback = function()
        loadstring(game:HttpGet("https://gist.githubusercontent.com/ardenmasster/9ba098c784ccc23a159adffe4730137e/raw/3031e96cef74f70f57c2eea9ed86ea8045ea7867/gistfile1.txt"))()
    end
})

MainTab:CreateButton({
    Name = "4. Tàng Hình",
    Callback = function()
        loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-Awesome-Invisible-man-21074"))()
    end
})

MainTab:CreateButton({
    Name = "5. Đi Bộ Trên Không",
    Callback = function()
        loadstring(game:HttpGet('https://raw.githubusercontent.com/GhostPlayer352/Test4/main/Float'))()
    end
})

MainTab:CreateButton({
    Name = "6. Menu Bay",
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/XNEOFF/FlyGuiV3/main/FlyGuiV3.txt"))()
    end
})

MainTab:CreateButton({
    Name = "7. Cho vật phẩm jerk off",
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/Sakupenny/Universal-Jerk-Off/refs/heads/main/Main.lua"))()
        Rayfield:Notify({Title = "Thông báo", Content = "Đã kích hoạt vật phẩm", Duration = 2})
    end
})

MainTab:CreateButton({
    Name = "8. Chống Treo Máy (Anti-AFK)",
    Callback = function()
        player.Idled:Connect(function()
            VirtualUser:Button2Down(Vector2.new(0,0),workspace.CurrentCamera.CFrame)
            task.wait(1)
            VirtualUser:Button2Up(Vector2.new(0,0),workspace.CurrentCamera.CFrame)
        end)
        Rayfield:Notify({Title = "Hệ thống", Content = "Đã kích hoạt Anti-AFK", Duration = 2})
    end
})

MainTab:CreateSection("Môi Trường")

MainTab:CreateButton({
    Name = "9. Làm Sáng Map",
    Callback = function()
         Lighting.Brightness = 2
         Lighting.ClockTime = 14
         Lighting.GlobalShadows = false
    end
})

MainTab:CreateButton({
    Name = "10. Siêu Nhảy Cao",
    Callback = function()
        if player.Character and player.Character:FindFirstChildOfClass("Humanoid") then
            player.Character:FindFirstChildOfClass("Humanoid").JumpPower = 200
            player.Character:FindFirstChildOfClass("Humanoid").UseJumpPower = true
        end
    end
})

MainTab:CreateButton({
    Name = "11. Menu Dịch Chuyển",
    Callback = function()
        local savedPos = nil
        local gui = Instance.new("ScreenGui", player.PlayerGui)
        gui.Name = "TeleportMenu"
        gui.ResetOnSpawn = false
        
        local frame = Instance.new("Frame", gui)
        frame.Size = UDim2.new(0, 180, 0, 150)
        frame.Position = UDim2.new(1, -200, 0.4, 0)
        frame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
        frame.Active = true
        frame.Draggable = true

        local save = Instance.new("TextButton", frame)
        save.Size = UDim2.new(1, -20, 0, 40)
        save.Position = UDim2.new(0, 10, 0, 20)
        save.Text = "Lưu Vị Trí"

        local tp = Instance.new("TextButton", frame)
        tp.Size = UDim2.new(1, -20, 0, 40)
        tp.Position = UDim2.new(0, 10, 0, 80)
        tp.Text = "Dịch Chuyển"

        save.MouseButton1Click:Connect(function()
            if player.Character then
                savedPos = player.Character.HumanoidRootPart.CFrame
                Rayfield:Notify({Title = "TP", Content = "Đã lưu!", Duration = 1})
            end
        end)

        tp.MouseButton1Click:Connect(function()
            if player.Character and savedPos then
                player.Character.HumanoidRootPart.CFrame = savedPos
            end
        end)
    end
})

MainTab:CreateButton({
    Name = "12. Tua Ngược Thời Gian",
    Callback = function()
        loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-Flashback-Script-26745"))()
    end
})

---------------------------------------------------------
-- [[ 2. TAB PHỤ: TÙY CHỈNH CHỈ SỐ ]] --
---------------------------------------------------------
ExtraTab:CreateSection("Tùy Chỉnh Nhân Vật")

ExtraTab:CreateSlider({
    Name = "Tốc độ chạy",
    Range = {16, 500},
    Increment = 1,
    CurrentValue = 16,
    Callback = function(Value)
        if player.Character and player.Character:FindFirstChild("Humanoid") then
            player.Character.Humanoid.WalkSpeed = Value
        end
    end,
})

ExtraTab:CreateSlider({
    Name = "Độ cao nhảy",
    Range = {50, 800},
    Increment = 1,
    CurrentValue = 50,
    Callback = function(Value)
        if player.Character and player.Character:FindFirstChild("Humanoid") then
            player.Character.Humanoid.UseJumpPower = true
            player.Character.Humanoid.JumpPower = Value
        end
    end,
})

ExtraTab:CreateSection("Chiến Đấu")

ExtraTab:CreateSlider({
    Name = "Hitbox",
    Range = {1, 10},
    Increment = 1,
    CurrentValue = 1,
    Callback = function(Value)
        _G.HitboxSize = Value
        _G.HitboxEnabled = true
        spawn(function()
            while _G.HitboxEnabled do
                for _, v in pairs(Players:GetPlayers()) do
                    if v ~= player and v.Character and v.Character:FindFirstChild("HumanoidRootPart") then
                        v.Character.HumanoidRootPart.Size = Vector3.new(_G.HitboxSize, _G.HitboxSize, _G.HitboxSize)
                        v.Character.HumanoidRootPart.Transparency = 0.7
                        v.Character.HumanoidRootPart.BrickColor = BrickColor.new("Really blue")
                        v.Character.HumanoidRootPart.CanCollide = false
                    end
                end
                task.wait(1)
            end
        end)
    end,
})

---------------------------------------------------------
-- [[ 3. TAB MÁY CHỦ: QUẢN LÝ SERVER ]] --
---------------------------------------------------------
ServerTab:CreateSection("Kết Nối")

ServerTab:CreateButton({
    Name = "Vào Lại Server",
    Callback = function()
        TeleportService:Teleport(game.PlaceId, player)
    end
})

ServerTab:CreateButton({
    Name = "Đổi Server",
    Callback = function()
        local list = HttpService:JSONDecode(game:HttpGet("https://games.roblox.com/v1/games/" .. game.PlaceId .. "/servers/Public?sortOrder=Asc&limit=100"))
        for _, v in pairs(list.data) do
            if v.playing < v.maxPlayers and v.playing >= 1 then
                TeleportService:TeleportToPlaceInstance(game.PlaceId, v.id, player)
                break
            end
        end
    end
})

ServerTab:CreateSection("Hiệu Suất")

ServerTab:CreateButton({
    Name = "Mở Khóa FPS",
    Callback = function()
        if setfpscap then
            setfpscap(999)
            Rayfield:Notify({Title = "Thành công", Content = "FPS đã mở lên 999", Duration = 2})
        end
    end
})

---------------------------------------------------------
-- [[ 4. TAB CÀI ĐẶT: THÔNG TIN ADMIN ]] --
---------------------------------------------------------
SettingTab:CreateSection("Thông Tin Nhà Phát Triển")

SettingTab:CreateLabel("👤 Chủ Sở Hữu: Quan_blacklock")
SettingTab:CreateLabel("👨‍💻 Admin/Dev: Iro")
SettingTab:CreateLabel("🛠️ Trạng thái: Đã lưu ghim toàn bộ code")
SettingTab:CreateLabel("🚀 Phiên Bản: V1.3 Stable (Final)")

SettingTab:CreateSection("Quản Lý Hệ Thống")

SettingTab:CreateButton({
    Name = "Đóng Menu",
    Callback = function()
        Rayfield:Destroy()
    end
})

---------------------------------------------------------
-- [ THÔNG BÁO KHỞI CHẠY ] --
---------------------------------------------------------
Rayfield:Notify({
    Title = "Quân Hub V1.3",
    Content = "Chào Admin Iro! Đã load 340+ dòng code.",
    Duration = 5
})

-- KẾT THÚC SCRIPT --
